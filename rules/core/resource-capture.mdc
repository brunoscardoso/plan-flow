---
description: "Resource capture system - during skill execution, watches for valuable reference materials and prompts the user before saving to flow/resources/. Include when executing any plan-flow skill."
alwaysApply: false
---

# Resource Capture

## Purpose

During any skill execution, the LLM watches for information that could be valuable for future development and prompts the user before saving it to `flow/resources/`. Resources are reference materials discovered during work — not artifacts of the workflow itself.

**Location**: `flow/resources/`

---

## Behavior

### During Any Skill Execution

While executing any plan-flow skill, watch for information that meets the capture criteria below.

When you identify something worth preserving:

1. **Pause** before moving on
2. **Ask the user**: "I found something that could be useful for future reference: _{brief description}_. Should I save it to `flow/resources/`?"
3. **If the user approves**: Write the file to `flow/resources/{name}.md`
4. **If the user declines**: Continue without saving — do not ask again for the same item

### What to Capture

| Category | Examples | Priority |
|----------|----------|----------|
| API documentation excerpts | Endpoint specs, auth flows, rate limits | High |
| Architecture diagrams/notes | System boundaries, data flows, service maps | High |
| Configuration references | Complex config patterns, env var maps, feature flags | Medium |
| Migration guides | Step-by-step procedures for DB migrations, API upgrades | Medium |
| Third-party integration notes | SDK quirks, undocumented behavior, workarounds | High |
| Performance benchmarks | Baseline measurements, optimization results | Medium |
| Security considerations | Auth flows, permission models, vulnerability notes | High |
| Domain knowledge | Business rules, data models, terminology glossaries | Medium |

### What NOT to Capture

- Artifacts that belong elsewhere (plans, discoveries, contracts, etc.)
- Generic knowledge (standard library docs, common patterns)
- Temporary debugging info (stack traces, one-off error logs)
- Duplicate information (already in brain, ledger, or other flow files)
- Code snippets without context (raw code belongs in the codebase)

---

## File Format

```markdown
# {Resource Title}

**Project**: [[{project-name}]]
**Captured**: {YYYY-MM-DD}
**Source**: {skill that discovered it}
**Category**: {api-docs|architecture|config|migration|integration|performance|security|domain}

## Summary

{One paragraph explaining what this resource is and why it's useful}

## Content

{The actual reference material}

## Related

- [[{feature-name}]] — {how it relates}
```

### File Naming

- **Format**: `{kebab-case-descriptive-name}.md`
- **Examples**: `stripe-webhook-verification.md`, `user-permissions-model.md`

---

## Rules

1. **Always ask first**: Never save a resource without user confirmation
2. **One prompt per resource**: Ask individually, don't batch
3. **Don't interrupt flow**: Ask at natural break points
4. **Brief ask**: One sentence describing what you found and why it matters
5. **No duplicates**: Check `flow/resources/` before suggesting
6. **Wiki-link everything**: Include `[[wiki-links]]` for Obsidian graph connectivity
7. **Silent on decline**: If the user says no, move on
